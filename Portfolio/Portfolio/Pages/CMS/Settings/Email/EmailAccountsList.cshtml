@page
@model Portfolio.Pages.CMS.Settings.Email.EmailAccountsListModel
@{
    ViewData["Title"] = "EmailAccountsList";
}

    <div class="container">


<h1>EmailAccountsList</h1>

<table class="table">
    @foreach (var account in Model.EmailAccounts)
    {
        <tr>
            <td>@account.FromName</td>
            <td>@account.FromAddress</td>
            <td><a asp-page="./EmailSetup" asp-route-skillId="@account.Id"><i class="far fa-edit fa-sm"></i></a></td>
        </tr>
    }

</table>
<a class="btn btn-outline-success" asp-page="./EmailSetup">Add</a>

    </div>
    